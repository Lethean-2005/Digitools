# Where the FastAPI backend lives. In dev this defaults to http://localhost:8001.
# In production point it at your deployed backend, e.g.:
#   VITE_API_BASE=https://api.digitools.example.com
VITE_API_BASE=http://localhost:8001
